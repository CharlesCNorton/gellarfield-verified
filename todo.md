1. Replace `unbreached_zero_exposure` (line 835). Restate with a time-bounded hypothesis: given `zb` where `zb t' (crew_zone c) = false` for all `t' < T`, prove `exposure field_states zb c T == 0`. The current hypothesis `false = true -> False` is vacuously true and the proof must depend on its hypothesis to qualify.

2. Constrain `flickering_effective` amplitude structurally. Bundle `base`, `amplitude`, and the four range proofs (`(1#4) < base`, `base <= (3#4)`, `0 < amplitude`, `base - amplitude <= (1#4)`) into a record `FlickerParams`. Rewrite `flickering_effective` and all downstream flickering lemmas to take this record. Bare unconstrained `Q` amplitudes with ad-hoc hypotheses on each lemma are not acceptable.

3. Prove `classify_field` monotonicity in the degradation direction: `s1 <= s2 -> classify_field s2 = Collapsed -> classify_field s1 = Collapsed`, and the analogous Flickering-or-worse variant. Replaces the trivial `classify_exhaustive` which is true of any three-branch match by construction.

4. Prove incursion monotonicity in field degradation. Define a `FieldState` ordering (`Active > Flickering > Collapsed`). Prove: `field_worse_eq fs1 fs2 -> incursion_possible fs2 wc dc = true -> incursion_possible fs1 wc dc = true`. This replaces `incursion_decidable`, which is trivially true of all `bool` functions.

5. Prove `navigator_alive` equivalence: `navigator_alive (mkNavigator (navigator_sanity init schedule t)) = true <-> ~ navigator_dead init schedule t`. This forces the unused `navigator_alive` (line 347) into the proof chain. A theorem that mentions `navigator_alive` but whose proof never unfolds it is not acceptable.

6. Refactor `field_strength_nonneg`/`fuel_nonneg`/`navigator_sanity_nonneg` and their `_nonincreasing` counterparts into instantiations of a generic clamped-decay lemma parameterized over the drain rate function. The generic version must be stated once and proved once. The six original lemma names must remain as aliases to instantiations. Six structurally identical proofs copy-pasted across three models are not acceptable.

7. Define `zone_breached_at` from `breach_time`: `zone_breached_at bulkheads t z := Nat.leb (breach_time bulkheads z) t`. Prove monotonicity in `t` (once breached, stays breached). Prove that `exposure` computed with `zone_breached_at` and with `breach_time` produce consistent results. The current state where `breach_time` and `zone_breach_fn` are parallel models that never interact is not acceptable.

8. Bridge `flickering_effective` to `field_strength` and `classify_field`. Prove: if `classify_field (field_strength init schedule t) = Flickering`, there exist `FlickerParams` such that `flickering_effective` with those params characterizes the effective field behavior at `t`. A standalone square-wave model disconnected from the decay model is not acceptable as the sole flickering representation.

9. Bridge `fueled_strength` into the corruption chain. Prove: `fuel init_fuel schedule T <= 0 -> forall T', (T <= T')%nat -> fueled_strength init init_fuel schedule T' = 0`. Then prove: `fueled_strength init init_fuel schedule T' = 0 -> classify_field (fueled_strength init init_fuel schedule T') = Collapsed`. The proof must compose `fueled_no_fuel_no_field` with `fuel_nonincreasing` and `classify_collapsed`. A `fueled_strength` that is defined, proved once, and never referenced again is not acceptable.

10. Bridge `incursion_possible` to a downstream consequence. Either (a) make `warp_flux` return nonzero only when `incursion_possible` is true for some daemon class and prove the equivalence, or (b) define `incursion_causes_breach` connecting the incursion predicate to zone breach propagation, or (c) prove that every scenario producing nonzero `warp_flux` has `incursion_possible = true` for at least one daemon class. An incursion lookup table that nothing in the corruption chain ever consults is not acceptable.

11. Connect `CourseCorrection` to fuel consumption. Prove: fuel consumed over `guided_duration cc base_dur` steps under warp condition `w` is strictly less than fuel consumed over `base_dur` steps under the same `w`, for `base_dur >= 2`. The proof must quantify the savings as a function of `cc_num`/`cc_den`. A `guided_shorter` that bounds only duration without connecting to fuel or survival is not acceptable.

12. Prove a `ShipState` theorem that connects at least three of its four fields (`ship_navigator`, `ship_fuel`, `ship_field_init`, `ship_num_zones`). E.g.: given sufficient fuel, navigator alive, and `ship_num_zones` bulkheads intact, transit under Calm preserves zero corruption for all crew in the interior zone. A theorem that pattern-matches `ShipState` and uses only one field is not acceptable.

13. Prove field strength eventually reaches Collapsed under sustained positive drain: `forall init schedule, 0 < init -> (forall t, 0 < drain (schedule t)) -> exists T, classify_field (field_strength init schedule T) = Collapsed`. The proof must provide a concrete witness `T`, not an opaque classical existence. The current file implies this via `field_strength_nonincreasing` and `drain_pos` but never states it.

14. Prove fuel exhaustion implies field collapse. Compose items 9 and the existing `fuel_exhaustion_storm` into: `forall init init_fuel, 0 < init -> 0 <= init_fuel -> exists T, classify_field (fueled_strength init init_fuel (fun _ => Storm) T) = Collapsed`. The proof must explicitly chain fuel exhaustion through `fueled_strength` to `classify_collapsed`. An existential that assumes collapse as a hypothesis (as current `navigator_necessity` does) is not acceptable.

15. Prove storm escalation cross-time: `forall init_fuel T, 0 <= init_fuel -> fuel init_fuel (fun _ => Storm) T <= fuel init_fuel (fun _ => Calm) (10 * T)`. The proof must handle clamping at zero across multiple steps. The per-step ratio `storm_drain_10x_calm` alone is not acceptable — the file's own comment (line 1029) acknowledges this is unproved.

16. Prove at least one result for a non-constant warp schedule. E.g.: for a schedule that is `Calm` for `k` steps then `Storm` for `m` steps, prove `fuel init (fun t => if Nat.ltb t k then Calm else Storm) (k + m) <= init - (inject_Z (Z.of_nat k) * (1#100) + inject_Z (Z.of_nat m) * (1#10))` (or the clamped version). All results in the file currently use `fun _ => Storm` or `fun _ => Rift`. Constant-schedule-only characterizations are not acceptable as the sole schedule results.

17. Prove population-level corruption: under constant Collapsed + all-breached for duration `T`, `corruption_count crew (fun _ => Collapsed) (fun _ _ => true) T` equals the number of crew members whose `corruption_threshold` is less than `inject_Z (Z.of_nat T)`. This is the discrete constructive version of the spec's original item 13 ("fraction approaches 1 as d approaches infinity"). The current `corruption_count_nondecreasing` as the sole population result is not acceptable.

18. Rewrite `navigator_death_unbounded` (line 465). Define `can_translate : Q -> bool` (navigator with positive sanity can translate out of the warp). Prove: `navigator_dead init_sanity schedule t -> can_translate (navigator_sanity init_sanity schedule t) = false`. Then prove: when `can_translate` is false, for any proposed finite transit bound, the ship cannot guarantee exit. The current proof `exists (S bound). lia` is pure Peano arithmetic that mentions no navigator concept and is not acceptable.

19. Strengthen the four renamed duplicates. Each must have a strictly stronger conclusion or strictly weaker hypothesis than the lemma it wraps. Specifically: `primordial_always_penetrates_rift` must strengthen to "Primordial penetrates during Rift AND no other daemon class penetrates Active during any condition except Rift." `calm_active_impenetrable` must strengthen to "Active + Calm blocks all classes AND Active + Storm blocks Lesser and Greater." `safe_means_no_corruption` must strengthen to "zero exposure AND zero corruption_count for the full crew list." `doomed_means_all_corrupted` must strengthen to "for every crew member in a breached zone, corruption occurs, with an explicit time bound as a function of threshold." A proof body consisting solely of `exact <original_lemma>` is not acceptable for any of these.

20. Extend `CrewMember` or `exposure` to support time-dependent zone assignment. Define `crew_zone_at : CrewMember -> nat -> ShipZone` or parameterize `exposure` over a zone trajectory. Prove: a crew member who evacuates from breached zone `z1` to unbreached zone `z2` at time `t_evac` accumulates zero additional exposure after `t_evac`. A model where `crew_zone` is a fixed record field and crew cannot respond to breach propagation is not acceptable as the final model.

21. Rewrite `navigator_necessity` (line 933) as the actual four-link composition. The proof must invoke four named lemmas in sequence: (a) navigator dead implies cannot bound transit duration, (b) unbounded duration implies fuel exhaustion (`fuel_exhaustion_storm` or generalization), (c) fuel exhaustion implies field collapse (item 14), (d) field collapse implies corruption (`collapse_implies_corruption`). Each link must be an explicit `apply` or `exact` in the proof term. A proof that discards the `init_fuel` hypothesis and calls `collapse_implies_corruption` directly is not acceptable.

22. Prove the end-to-end doom chain: given a ship with finite fuel, navigator dead or absent, under sustained Storm or Rift, for every crew member in a zone that is eventually breached (per `breach_time`), there exists a constructive time `T` at which they are corrupted. The proof must thread through fuel to field to breach to exposure to corruption with each transition being an explicit lemma application. Provide the witness `T` as a computable function of the inputs.

23. Rewrite `classify_transit` to consume fuel depletion, navigator sanity, zone breach status, and incursion possibility — not just `field_strength` evaluated once. The classifier must check `fuel` at the given duration, check `navigator_sanity`, account for `zone_breached_at`, and evaluate `incursion_possible`. A classifier that uses `init_fuel` as field strength init (current line 1095) and ignores all other models is not acceptable.

24. Prove grand composition soundness: `classify_transit` returning `Safe` implies zero corruption for all crew in all zones; `Survivable` implies a computable upper bound on exposure for each crew member; `Doomed` implies the existence of a corruption time for every crew member in a breached zone. Each implication must be a separate proved theorem. An exhaustiveness proof over three constructors (`transit_outcome_exhaustive`) is not acceptable as the soundness result.

25. Prove deeper-zone survival advantage: for a linear ship with `n` zones and all bulkheads intact, using `zone_breached_at` from item 7, prove that a crew member in zone `z2 > z1` has strictly less exposure than a crew member in zone `z1` at any time `t` where `z1` is breached but `z2` is not. The proof must compose `all_intact_breach_time`, `zone_breached_at`, and `exposure` into a single inequality. This forces the three models to interact.
